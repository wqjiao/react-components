/**
 * 路由数据集合
 * Route json file(route name、path and component)
 */
import channelSide from './routeArray/channelSide'; // 渠道方管理

let routeJson = {
	channelSide
}

export default routeJson;
